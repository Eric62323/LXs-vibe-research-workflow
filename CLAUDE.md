# CLAUDE.md - Vibe Econ Research (Block Cycle v5.0)

**版本**：v5.0 (ARS增强版)
**核心架构**：4板块循环 (Framework -> Empirical -> Writing -> Review)
**增强来源**：Academic Research Skills (ARS v3.7 by Cheng-I Wu) + Vibe原创

---

## 📖 参考与引用 (References & Acknowledgments)

本工作流在开发过程中参考借鉴了以下项目：

1. **Academic Research Skills (ARS)** — by Cheng-I Wu
   - GitHub: https://github.com/Imbad0202/academic-research-skills
   - 版本：v3.7
   - 许可：MIT License
   - 借鉴内容：
     - 多角色审稿团队（5人+编辑综合人）与7维度评分体系
     - Devil's Advocate让步阈值协议与框架锁定检测
     - 苏格拉底式引导（SCR协议、承诺门、意图检测）
     - 完整性关卡（Integrity Gates）机制
     - Deep Research文献研究6阶段流水线与模式选择
     - 写作模式多样化（7种模式）与Fidelity-Originality光谱
     - 风格校准、图表验证、论断溯源审计
     - 认知框架（Toulmin论证模型、Bradford Hill因果推理、IBE）
     - 写作质量检查（AI高频词、标点控制、废话检测）
     - R&R可追溯性矩阵与分数轨迹追踪
     - 反泄漏协议与认知状态标注

2. **Karpathy's AI Coding Principles** — by Andrej Karpathy
   - 借鉴内容：5条AI行为准则（先思考再动笔、简洁优先、精准修改、目标驱动、错误不掩盖）

---

## 🚀 快速启动

**每次对话，AI检测**：
1. `项目/` 存在吗？
2. 读取 `项目信息.md`：
   - 确认 **产出类型** (期刊/开题/学位) → 决定侧重点。
   - 确认 **Cycle** 和 **Block**。
3. 询问："准备进入 [当前板块] 吗？"

---

## 🧠 AI行为准则 (源自 Karpathy Skills)

以下5个原则已嵌入各Agent，适用于所有板块：

1. **先思考再动笔**：不假设、不隐藏困惑、呈现替代解释。当不确定时，问而非猜。→ 主嵌入 `/框架`
2. **简洁优先**：最简模型解决问题。无投机性假设。无多余术语。一个资深学者会说这过度复杂吗？→ 主嵌入 `/审查`
3. **精准修改**：只改任务相关的内容。匹配已有风格。发现无关问题→提及但不修改。→ 主嵌入 `/写作`
4. **目标驱动**：每步定义完成标准（verify checkpoint）。可验证才算完成。不可验证的步骤需拆解。→ 主嵌入 `/实证`
5. **错误不掩盖**：禁止 `capture` 吃掉错误。代码报错必须暴露、必须解决，而不是被静默跳过。目标是完美运行的代码，不是被黑箱包裹的假平静。`capture noisily` + `if _rc != 0 exit` 同样是掩盖——它把具体错误信息丢给了被调用的子文件日志，主程序只看到一个抽象的"失败了"，失去了追踪错误链条的能力。→ 适用于所有 Stata 代码。

---

## 🔒 IRON RULE (不可违反 — 所有Agent通用)

以下规则来自ARS，在长对话中也不能违反。**每次板块转换时重新注入**：

1. **不编造文献**：永远不编造、虚构或猜测文献信息。只提供检索线索。
2. **反泄漏协议**：优先使用会话材料，发现材料缺失时标记 `[MATERIAL GAP]`，而非用LLM参数记忆填补。
3. **结果诚实**：不管结果是否支持假设，都必须诚实报告。
4. **不掩盖错误**：代码报错必须暴露并解决。
5. **认知状态标注**：陈述论点时自觉标注认知状态（Known/Probable/Plausible/Unknown）。

**各Agent的专属IRON RULE**：详见各Agent文件中的"IRON RULE"部分。

---

## 🚫 Anti-Pattern 警告 (所有Agent通用)

| Anti-Pattern | 表现 | 正确行为 |
|-------------|------|---------|
| 谄媚性让步 | AI在受到质疑时过快让步 | 严格按DA让步阈值协议（仅≥4分才让步） |
| 框架锁定 | AI在用户框架内"打转"，不质疑前提 | 每轮必须包含前提级质疑 |
| 过早收敛 | 用户还在探索时就尝试产出 | 检测意图，探索阶段保持开放 |
| 材料幻觉 | 用参数记忆填补用户未提供的材料 | 标记 `[MATERIAL GAP]` |
| 顺便修改 | 修改A时"顺便"改了B | 严格聚焦当前任务 |

**各Agent的专属Anti-Pattern**：详见各Agent文件中的"Anti-Pattern 警告"部分。

---

## 📋 迭代记录规则

**规则**：每次对项目进行重要修改（数据清洗、代码重构、变量定义变更、模型规格调整、文件结构调整等），必须在项目根目录的 `迭代研究日志.md` 中追加记录。

**记录格式**：
- 以 `## 迭代N：简短标题（日期）` 为标题
- 包含：问题/背景、修改位置（文件+行号）、修复代码、验证结果
- 每次修改后同步更新日志，不等到会话结束批量补记

**必须包含"当前卡点"板块**：
每次更新迭代日志时，必须在末尾记录当前卡点：

```markdown
## 当前卡点
- **卡点描述**：[当前阻碍进展的问题]
- **卡点类型**：[数据问题 / 代码错误 / 理论困惑 / 方法选择 / 外部依赖]
- **尝试过的方案**：[已经尝试过什么]
- **待讨论**：[需要和AI/导师/同行讨论什么]
- **影响范围**：[这个卡点阻塞了哪些后续步骤]
```

如果当前没有卡点，写"无阻塞"即可。卡点更新的意义：确保每次对话恢复时，AI能立即定位到未解决的问题，避免重复已完成的工作或遗漏关键障碍。

**重要修改的定义**（满足任一即需记录）：
1. 数据清洗：发现并修复异常值、缺失值、编码错误
2. 代码逻辑变更：修改 do-file 中的变量生成、筛选条件、合并方式
3. 模型规格调整：新增/删除控制变量、更换 IV、修改固定效应层级
4. 文件结构变更：新增/删除/重命名 do-file 或关键数据文件
5. 数据质量审计：系统性扫描后的发现与修复

**无需记录**：临时调试代码、路径调整、日志文件、临时文件、纯格式修改。

---

## 📁 文件组织规则

**项目文件夹**：`/Users/liuxin/Desktop/rural_LU/`
**工作流文件夹**：`/Users/liuxin/.claude/skills/vibe-research-workflow/`

### 文件归属

| 文件类型 | 存放位置 | 说明 |
|----------|----------|------|
| Stata 代码 (.do) | `项目/dofile/` | 数据清洗、分析、图表 |
| 数据 (.dta) | `项目/savedata/` | 中间数据和最终数据 |
| 图表 (.png/.pdf/.gph) | `项目/figure/` | 论文插图、可视化 |
| 结果表格 (.csv/.xlsx) | `项目/table/` | 回归结果、描述统计 |
| Stata 日志 (.log) | `项目/log/` | 所有 do-file 的运行日志 |
| 论文文稿 (.docx/.md) | `项目/paper/` | 正文、图表、附录 |
| 迭代日志 | `项目/迭代研究日志.md` | 项目唯一迭代记录 |
| 工作流规则 | `工作流/` (本目录) | CLAUDE.md, Agent 文件, skills |
| 临时文件 | `项目/tmp/` | 用完即删 |

**核心原则**：所有生成的文件（代码、数据、日志、表格、临时文件）**必须在项目文件夹内**。禁止在项目文件夹之外（包括 `/tmp/`、`~`、工作流文件夹等）创建或存储任何项目相关文件。

### Stata 运行规则

运行 Stata 时必须使用 `--cwd` 指定项目根目录，确保日志写入 `项目/log/`：

```bash
stata-cli --cwd /Users/liuxin/Desktop/rural_LU run-file xxx.do
stata-cli --cwd /Users/liuxin/Desktop/rural_LU run "sum age"
```

**vibe-research-workflow 文件夹只保存工作流规则相关文件**，不保存项目数据、代码、日志或论文。

---

## 🔄 核心循环 (The Loop)

> **原则**：所有板块必须严格遵循其Agent文件中定义的 **"标准工作流 (Standard Workflow)"**。允许步骤内循环，但不允许跳过关键步骤。

### Block 1: 概念框架 (`/框架`)
- **任务**：定义理性(Why)与严谨(How)。嵌入苏格拉底式引导。
- **输出**：`概念框架.md` (v0.X)。
- **Agent**：`_系统/概念框架Agent.md`。
- **认知框架**：`_系统/规范/论证推理框架.md`（Toulmin模型、Bradford Hill因果推理、IBE、认知状态分类）

### Block 2: 实证检验 (`/实证`)
- **任务**：从数据清洗到计量分析的完整实证流程。
- **Phase 结构**：
  - Phase 0: 数据清洗（01a→01b→01c）
  - Phase 1: 数据审查 + 理解确认 + EDA + 描述统计（含论文类型标注）
  - Phase 2: 主回归 + VERIFY 自动检查
  - Phase 3: 异质性分析
  - Phase 4: 稳健性检验
  - 完整性关卡（量化评分，≥80 分通过）
- **输出**：`实证结果.md` 或 修正意见。
- **Agent**：`_系统/实证检验Agent.md`。
- **参考**：`_系统/参考/论文类型标准清单.md`（6 种论文类型的审稿人期望清单）。
- **🚪 完整性关卡**：退出前必须通过量化评分关卡（变量一致性、样本量、可复现性、统计诊断、假设对应、论文类型清单核查，≥80 分通过）。

### Block 3: 写作输出 (`/写作`)
- **任务**：将素材转化为学术文本。自动执行写作质量检查。
- **输出**：`论文章节` 卡片。
- **Agent**：`_系统/学术写作Agent.md`。
- **认知框架**：`_系统/规范/写作判断框架.md`（电梯测试、读者旅程、学科语调、修订决策矩阵）

### Block 4: 论证审查 (`/审查`) ⭐ **ARS大幅增强**
- **任务**：多角色审稿 + 苏格拉底辅导 + 修改 + 再审查（内部子循环）。
- **审稿团队**：Editor-in-Chief + Methodology Reviewer + Domain Reviewer + Perspective Reviewer + Devil's Advocate + Editorial Synthesizer
- **评分体系**：7维度 × 0-100分（方法论严谨性、文献覆盖、论证强度、写作质量、创新性、可复现性、影响力）
- **反谄媚机制**：DA让步阈值协议 + 框架锁定检测
- **Agent**：`_系统/论证审查Agent.md`。
- **认知框架**：`_系统/规范/审稿质量思维框架.md`（三重透镜、校准问题、常见审稿陷阱）

---

## 🚪 完整性关卡 (Integrity Gates) ⭐ **ARS新增**

**不可跳过的强制性关卡**，位于关键节点：

```
/框架(步骤1) → [关卡2.5:文献完整性] → /框架(步骤3-4) → [关卡4.5:框架→实证] → /实证 → [关卡:实证完整性] → /写作 → /审查 → [关卡:审查通过] → 下一Cycle
                                                                                                                      ↑                    |
                                                                                                                      └── 未通过（最多3轮）
```

### 关卡2.5：文献研究完整性关卡（步骤2→步骤3）
- **位置**：概念框架Agent，文献研究完成后
- **检查项**：核心文献覆盖、Gap陈述基于证据、边际贡献明确、搜索策略已文档化
- **详见**：`_系统/概念框架Agent.md → 关卡2.5`

### 关卡4.5：框架→实证完整性关卡（步骤4→/实证）
- **位置**：概念框架Agent全部步骤完成后，进入实证前
- **检查项**：研究问题明确、假设已陈述、理论逻辑闭环、DAG已绘制、识别策略有依据、内生性已讨论、数据需求已明确
- **详见**：`_系统/概念框架Agent.md → 关卡4.5`

### 关卡：实证完整性关卡（`/实证` → `/写作`）
- **位置**：实证检验Agent退出前
- **检查项**：变量一致性、样本量验证、结果可复现性、统计诊断完整性、结果与假设对应、论文类型清单核查
- **评分**：量化评分（满分 100），≥80 分通过，60-79 需修正，<60 回到对应 Phase
- **详见**：`_系统/实证检验Agent.md → 实证完整性关卡`

### 关卡：审查通过关卡（`/审查` → 下一Cycle）
- **位置**：论证审查Agent子循环通过后
- **条件**：所有7维度评分≥75 且 无分数回退
- **详见**：`_系统/论证审查Agent.md → Phase 4: 再审查`

---

## 🔄 板块转换强化协议 (Block Transition Reinforcement) ⭐ **ARS新增**

**每次从一个板块切换到另一个板块时，必须执行以下强化步骤**：

1. **重新注入IRON RULE**：提醒当前Agent的核心不可违反规则
2. **重新注入Anti-Pattern**：提醒当前Agent的关键失败模式
3. **位置标注**：输出当前位置摘要（板块/步骤/轮次）
4. **上下文传递**：确认前一板块的输出已正确传递到当前板块
5. **完成标准确认**：确认当前板块的完成标准

---

## 🎯 交互指令

| 指令 | 对应板块 | 功能 |
|------|---------|------|
| **`/框架`** | **概念框架** | 定义问题、修补理论镜头 |
| **`/实证`** | **实证检验** | 解释回归表格、验证假设 |
| **`/写作`** | **写作输出** | 撰写章节、润色语言 |
| **`/审查`** | **论证审查** | **多角色审稿**、评分、苏格拉底辅导、修改指导 |
| **`/主干`** | **项目管理** | 查看进度、回归当前循环 |
| **`/成稿`** | **论文生成** | 拼接所有章节 |

---

## 📝 循环进阶机制

**Cycle 1: 原型 (Prototype)**
- 粗略框架 -> 核心结果 -> 粗略草稿 -> 逻辑通顺性检查。

**Cycle 2: 细化 (Refinement)**
- 理论深化 -> 稳健性检验 -> 精细写作 -> 证据充分性检查。

**审查通过 (Review Passed)** = 所有7维度评分≥75 且 无分数回退 = 进入下一 Cycle 的钥匙。

---

## 🔧 自动检测逻辑

1. 读取 `项目信息.md`。
2. 确认 `当前循环` 和 `当前板块`。
3. 加载对应 Agent。

---

## 🧭 对话前位置标注（新增）

**目标**：每次对话开始前，输出“当前位置摘要”，包含已完成/未完成步骤与回到该步骤的轮次。

**输出模板**：
- 当前位置：[板块] / [步骤]
- 已完成：步骤A、步骤B
- 未完成：步骤C、步骤D
- 回到该步骤：第 N 次

**实现规则**：
1. 读取 `项目信息.md` 与对应板块的阶段记录。
2. 若步骤有多轮迭代，记录 `步骤轮次` 并在对话前展示。
3. 步骤完成后，标记完成并推进到下一步。

---

## 📊 新增功能（2026-02-07）

### **三线表格式规范** ⭐
- **描述性统计表格**：必须使用三线表格式（顶线、栏目线、底线，无竖线）
- **回归结果表格**：必须显示样本量、标准误、显著性标识
- **Stata代码**：使用`putdocx`命令，设置`border(all, nil)`移除竖线
- **详见**：`_系统/学术写作Agent.md → 步骤5：表格规范化`

### **样本量分析** ⭐
- **回归前**：诊断样本量充足性（N ≥ 400为理想，N ≥ 100为最低要求）
- **回归后**：报告实际使用样本量、未使用样本量、固定效应吸收情况
- **缺失值分析**：统计各变量缺失比例，识别样本流失原因
- **详见**：`_系统/实证检验Agent.md → 步骤1.2.5 样本量与数据质量审查`

---
当AI突破对话框，使用AI的方式不再是chat only。就会对AI产生更多的期待：“既然他知道，那让他直接去做好了”。AI Agent应运而生。agent是一个伟大的进步，让AI不再仅仅是“隔岸观火”，而是真的“下场灭火”，能够执行动作，能够调用其他的工具，开始尝试挑战“人”的定义。

但是这个过程还不能“一键实现”。第一个难关在于LLM（大语言模型）的概率性。LLM强大的联想能力和创造力是它的优势，但也是它的挑战。它可能会产生一些不受控的输出，或者在执行任务时偏离预期的轨道。很多时候，我们希望AI能够足够发散，产生创新性的想法，但又希望它能够聚焦在特定的任务上，提供准确的输出。这就需要足够明确的指令和结构化的流程、甚至是足够发挥不同LLM特点的工作分工，来引导AI的输出。

管理LLM第二个难关是有限的上下文，例如GPT-4的上下文窗口为8K tokens，国产GLM的上下文容量为120K，最新的claude opus 4.6增加到了200K，可以通俗的将一个token理解为一个汉字，但是这对于一个复杂的研究项目来说可能还是远远不够。因此，我们需要设计一个系统，能够在不同阶段和不同任务之间有效地管理信息流，确保AI能够访问到它需要的信息，同时又不会被过多无关的信息干扰。
可是由于AI的概率性和上下文限制，它变成了一个不那么可控的员工。这时候就需要建立起严格的规章制度，发给他一本员工手册，让他严格按照公司流程办事，这就是skill。此后，还会加入多agent协同和系统工程管理的概念，来进一步提升AI的效率和协同能力。

接入AI工具主要包括四个方面，一是idea，二是数据处理，三是文字，四是审查校验。在每个环节还有很多细节任务。

对vibe research工作流的展示，第一步应该从代码补齐开始，这也是当前AI code功能最擅长的领域。

对代码补齐的延伸，包括代码的重构和优化，甚至是自动化生成Stata代码来实现特定的分析任务，这些都是AI可以大显身手的地方。
如果AI能够补齐代码，而LLM是大语言模型，天生能够适应自然语言，那么AI辅助写作，尤其是学术写作，应该是AI最擅长的领域之一。我们可以让AI根据我们的研究框架和实证结果，自动生成符合学术规范的论文段落，甚至是整章内容。
最后，AI的审查和校验功能也非常重要。我们可以让AI帮助我们检查论文的逻辑结构，寻找潜在的漏洞，甚至是提供文献检索的线索，帮助我们找到支持或反驳我们论点的文献。

这些仅仅是小的流程环节上的个别功能，我们还需要一个整体的系统来管理这些功能，确保它们能够在正确的时间被正确地调用，并且能够有效地协同工作。这就是我们设计这个Vibe Research Workflow的初衷，希望能够通过一个结构化的流程和明确的指令，来引导AI在不同阶段发挥它的优势，同时又能够保持研究的严谨性和创新性。


**Ready for the Loop.**
