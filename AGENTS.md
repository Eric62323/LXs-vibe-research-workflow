# AGENTS.md - Vibe Econ Research (Block Cycle v4.0)

**版本**：v4.0 (The Vibe Loop)
**核心架构**：4板块循环 (Framework -> Empirical -> Writing -> Review)

---

## 🚀 快速启动

**每次对话，AI检测**：
1. `项目/` 存在吗？
2. 读取 `项目信息.md`：
   - 确认 **产出类型** (期刊/开题/学位) → 决定侧重点。
   - 确认 **Cycle** 和 **Block**。
3. 询问："准备进入 [当前板块] 吗？"

---

## 🧠 AI行为准则 (源自 Karpathy Skills)

以下4个原则已嵌入各Agent，适用于所有板块：

1. **先思考再动笔**：不假设、不隐藏困惑、呈现替代解释。当不确定时，问而非猜。→ 主嵌入 `/框架`
2. **简洁优先**：最简模型解决问题。无投机性假设。无多余术语。一个资深学者会说这过度复杂吗？→ 主嵌入 `/审查`
3. **精准修改**：只改任务相关的内容。匹配已有风格。发现无关问题→提及但不修改。→ 主嵌入 `/写作`
4. **目标驱动**：每步定义完成标准（verify checkpoint）。可验证才算完成。不可验证的步骤需拆解。→ 主嵌入 `/实证`

---

## 📋 迭代记录规则

**规则**：每次对项目进行重要修改（数据清洗、代码重构、变量定义变更、模型规格调整、文件结构调整等），必须在项目根目录的 `迭代研究日志.md` 中追加记录。

**记录格式**：
- 以 `## 迭代N：简短标题（日期）` 为标题
- 包含：问题/背景、修改位置（文件+行号）、修复代码、验证结果
- 每次修改后同步更新日志，不等到会话结束批量补记

**重要修改的定义**（满足任一即需记录）：
1. 数据清洗：发现并修复异常值、缺失值、编码错误
2. 代码逻辑变更：修改 do-file 中的变量生成、筛选条件、合并方式
3. 模型规格调整：新增/删除控制变量、更换 IV、修改固定效应层级
4. 文件结构变更：新增/删除/重命名 do-file 或关键数据文件
5. 数据质量审计：系统性扫描后的发现与修复

**无需记录**：临时调试代码、路径调整、日志文件、临时文件、纯格式修改。

---

## 📁 文件组织规则

**项目文件夹**：`/Users/liuxin/Desktop/LaborUnderutilization/`
**工作流文件夹**：`/Users/liuxin/.Codex/skills/vibe-research-workflow/`

### 文件归属

| 文件类型 | 存放位置 | 说明 |
|----------|----------|------|
| Stata 代码 (.do) | `项目/dofile/` | 数据清洗、分析、图表 |
| 数据 (.dta) | `项目/savedata/` | 中间数据和最终数据 |
| 图表 (.png/.pdf/.gph) | `项目/figure/` | 论文插图、可视化 |
| 结果表格 (.csv/.xlsx) | `项目/table/` | 回归结果、描述统计 |
| Stata 日志 (.log) | `项目/log/` | 所有 do-file 的运行日志 |
| 论文文稿 (.docx/.md) | `项目/paper/` | 正文、图表、附录 |
| 迭代日志 | `项目/迭代研究日志.md` | 项目唯一迭代记录 |
| 工作流规则 | `工作流/` (本目录) | AGENTS.md, Agent 文件, skills |
| 临时文件 | `项目/tmp/` | 用完即删 |

**核心原则**：所有生成的文件（代码、数据、日志、表格、临时文件）**必须在项目文件夹内**。禁止在项目文件夹之外（包括 `/tmp/`、`~`、工作流文件夹等）创建或存储任何项目相关文件。

### Stata 运行规则

运行 Stata 时必须使用 `--cwd` 指定项目根目录，确保日志写入 `项目/log/`：

```bash
stata-cli --cwd /Users/liuxin/Desktop/LaborUnderutilization run-file xxx.do
stata-cli --cwd /Users/liuxin/Desktop/LaborUnderutilization run "sum age"
```

**vibe-research-workflow 文件夹只保存工作流规则相关文件**，不保存项目数据、代码、日志或论文。

---

## 🔄 核心循环 (The Loop)

> **原则**：所有板块必须严格遵循其Agent文件中定义的 **"标准工作流 (Standard Workflow)"**。允许步骤内循环，但不允许跳过关键步骤。

### Block 1: 概念框架 (`/框架`)
- **任务**：定义理性(Why)与严谨(How)。
- **输出**：`概念框架.md` (v0.X)。
- **Agent**：`_系统/概念框架Agent.md`。

### Block 2: 实证检验 (`/实证`)
- **任务**：执行设计，解释Stata结果。
- **输出**：`实证结果.md` 或 修正意见。
- **Agent**：`_系统/实证检验Agent.md`。

### Block 3: 写作输出 (`/写作`)
- **任务**：将素材转化为学术文本。
- **输出**：`论文章节` 卡片。
- **Agent**：`_系统/学术写作Agent.md`。

### Block 4: 论证审查 (`/审查`)
- **任务**：**证据策略** + **逻辑审计**。
- **功能**：提供文献检索线索 (不读不存在的文献)，检查逻辑漏洞。
- **Agent**：`_系统/论证审查Agent.md` (原文献Agent)。

---

## 🎯 交互指令

| 指令 | 对应板块 | 功能 |
|------|---------|------|
| **`/框架`** | **概念框架** | 定义问题、修补理论镜头 |
| **`/实证`** | **实证检验** | 解释回归表格、验证假设 |
| **`/写作`** | **写作输出** | 撰写章节、润色语言 |
| **`/审查`** | **论证审查** | **找证据检索策略**、查逻辑 |
| **`/主干`** | **项目管理** | 查看进度、回归当前循环 |
| **`/成稿`** | **论文生成** | 拼接所有章节 |

---

## 📝 循环进阶机制

**Cycle 1: 原型 (Prototype)**
- 粗略框架 -> 核心结果 -> 粗略草稿 -> 逻辑通顺性检查。

**Cycle 2: 细化 (Refinement)**
- 理论深化 -> 稳健性检验 -> 精细写作 -> 证据充分性检查。

**审查通过 (Review Passed)** = 进入下一 Cycle 的钥匙。

---

## 🔧 自动检测逻辑

1. 读取 `项目信息.md`。
2. 确认 `当前循环` 和 `当前板块`。
3. 加载对应 Agent。

---

## 🧭 对话前位置标注（新增）

**目标**：每次对话开始前，输出“当前位置摘要”，包含已完成/未完成步骤与回到该步骤的轮次。

**输出模板**：
- 当前位置：[板块] / [步骤]
- 已完成：步骤A、步骤B
- 未完成：步骤C、步骤D
- 回到该步骤：第 N 次

**实现规则**：
1. 读取 `项目信息.md` 与对应板块的阶段记录。
2. 若步骤有多轮迭代，记录 `步骤轮次` 并在对话前展示。
3. 步骤完成后，标记完成并推进到下一步。

---

## 📊 新增功能（2026-02-07）

### **三线表格式规范** ⭐
- **描述性统计表格**：必须使用三线表格式（顶线、栏目线、底线，无竖线）
- **回归结果表格**：必须显示样本量、标准误、显著性标识
- **Stata代码**：使用`putdocx`命令，设置`border(all, nil)`移除竖线
- **详见**：`_系统/学术写作Agent.md → 步骤5：表格规范化`

### **样本量分析** ⭐
- **回归前**：诊断样本量充足性（N ≥ 400为理想，N ≥ 100为最低要求）
- **回归后**：报告实际使用样本量、未使用样本量、固定效应吸收情况
- **缺失值分析**：统计各变量缺失比例，识别样本流失原因
- **详见**：`_系统/实证检验Agent.md → 步骤1.2.5 样本量与数据质量审查`

---
当AI突破对话框，使用AI的方式不再是chat only。就会对AI产生更多的期待：“既然他知道，那让他直接去做好了”。AI Agent应运而生。agent是一个伟大的进步，让AI不再仅仅是“隔岸观火”，而是真的“下场灭火”，能够执行动作，能够调用其他的工具，开始尝试挑战“人”的定义。

但是这个过程还不能“一键实现”。第一个难关在于LLM（大语言模型）的概率性。LLM强大的联想能力和创造力是它的优势，但也是它的挑战。它可能会产生一些不受控的输出，或者在执行任务时偏离预期的轨道。很多时候，我们希望AI能够足够发散，产生创新性的想法，但又希望它能够聚焦在特定的任务上，提供准确的输出。这就需要足够明确的指令和结构化的流程、甚至是足够发挥不同LLM特点的工作分工，来引导AI的输出。

管理LLM第二个难关是有限的上下文，例如GPT-4的上下文窗口为8K tokens，国产GLM的上下文容量为120K，最新的Codex opus 4.6增加到了200K，可以通俗的将一个token理解为一个汉字，但是这对于一个复杂的研究项目来说可能还是远远不够。因此，我们需要设计一个系统，能够在不同阶段和不同任务之间有效地管理信息流，确保AI能够访问到它需要的信息，同时又不会被过多无关的信息干扰。
可是由于AI的概率性和上下文限制，它变成了一个不那么可控的员工。这时候就需要建立起严格的规章制度，发给他一本员工手册，让他严格按照公司流程办事，这就是skill。此后，还会加入多agent协同和系统工程管理的概念，来进一步提升AI的效率和协同能力。

接入AI工具主要包括四个方面，一是idea，二是数据处理，三是文字，四是审查校验。在每个环节还有很多细节任务。

对vibe research工作流的展示，第一步应该从代码补齐开始，这也是当前AI code功能最擅长的领域。

对代码补齐的延伸，包括代码的重构和优化，甚至是自动化生成Stata代码来实现特定的分析任务，这些都是AI可以大显身手的地方。
如果AI能够补齐代码，而LLM是大语言模型，天生能够适应自然语言，那么AI辅助写作，尤其是学术写作，应该是AI最擅长的领域之一。我们可以让AI根据我们的研究框架和实证结果，自动生成符合学术规范的论文段落，甚至是整章内容。
最后，AI的审查和校验功能也非常重要。我们可以让AI帮助我们检查论文的逻辑结构，寻找潜在的漏洞，甚至是提供文献检索的线索，帮助我们找到支持或反驳我们论点的文献。

这些仅仅是小的流程环节上的个别功能，我们还需要一个整体的系统来管理这些功能，确保它们能够在正确的时间被正确地调用，并且能够有效地协同工作。这就是我们设计这个Vibe Research Workflow的初衷，希望能够通过一个结构化的流程和明确的指令，来引导AI在不同阶段发挥它的优势，同时又能够保持研究的严谨性和创新性。


**Ready for the Loop.**
